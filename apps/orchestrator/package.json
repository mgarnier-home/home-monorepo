{
  "private": true,
  "name": "orchestrator",
  "scripts": {
    "patch-version": "npm version patch",
    "esbuild": "tsx scripts/build.ts",
    "lint": "tsc --project tsconfig.json --noEmit",
    "build": "npm run lint && npm run esbuild -- build",
    "dev": "tsx watch --tsconfig tsconfig.json --inspect --clear-screen=false src/main.ts ",
    "start": "npm run build && node dist/main.js"
  },
  "dependencies": {
    "@types/express": "^4.17.21",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "logger": "workspace:*",
    "utils": "workspace:*",
    "yaml": "^2.4.0",
    "zod": "^3.22.4"
  },
  "version": "0.0.2"
}
