{
  "private": true,
  "name": "autosaver",
  "scripts": {
    "patch-version": "npm version patch",
    "build": "npm run lint && npm run esbuild -- build",
    "dev": "tsx watch --tsconfig tsconfig.json --clear-screen=false src/main.ts ",
    "esbuild": "tsx scripts/server.ts",
    "lint": "tsc --project tsconfig.json --noEmit",
    "start": "npm run build && node dist/main.js"
  },
  "scripts-old": {
    "build": "shx rm -rf dist/ && tsc",
    "start": "nx run build && node dist/main.js",
    "patch-version": "npm version patch"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/node-cron": "^3.0.8",
    "@types/nodemailer": "^6.4.2"
  },
  "dependencies": {
    "console-table-printer": "^2.11.2",
    "cpy": "^10.1.0",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "generate-password": "^1.7.0",
    "logger": "workspace:*",
    "node-cron": "^3.0.2",
    "nodemailer": "^6.9.3",
    "ntfy-utils": "workspace:*",
    "utils": "workspace:*",
    "yaml": "^2.4.0"
  },
  "version": "2.0.0"
}
